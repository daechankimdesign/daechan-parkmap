# Copy this file to .env.local and fill in your own Mapbox access token.
# Get one at https://account.mapbox.com/access-tokens/ (a publishable "pk." token).
# Restrict it by URL in your Mapbox account before deploying anywhere public.
VITE_MAPBOX_TOKEN=
